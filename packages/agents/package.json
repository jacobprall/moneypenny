{
  "name": "@mp/agents",
  "version": "0.1.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --external bun:sqlite",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mp/db": "workspace:*",
    "@mp/loop": "workspace:*",
    "@mp/ctx": "workspace:*",
    "@mp/tools": "workspace:*",
    "gray-matter": "^4.0.3",
    "chokidar": "^4.0.3",
    "cron-parser": "^5.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "bun-types": "latest",
    "tsup": "^8.0.0",
    "typescript": "^5.7.0"
  }
}
