{
  "installCommand": "cd web && npm install && cd ../docs && npm install",
  "buildCommand": "cd web && npm run build && cd ../docs && npm run build && cp -r dist ../web/.vercel/output/static/docs && cd .. && rm -rf .vercel/output && mv web/.vercel/output .vercel/output"
}
