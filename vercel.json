{
  "installCommand": "cd web && npm install && cd ../docs && npm install",
  "buildCommand": "cd docs && npm run build && mkdir -p ../web/public/docs && cp -r dist/* ../web/public/docs/ && cd ../web && npm run build",
  "outputDirectory": "web/.vercel/output"
}
